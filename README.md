# Shhhh

This repository contains a Google username search tool.

## google_username_search.py

This Python script searches Google for occurrences of a username using the `googlesearch` package.

### Usage

```bash
python google_username_search.py username [-n NUM]
```

Install dependencies with `pip install google` if needed.
